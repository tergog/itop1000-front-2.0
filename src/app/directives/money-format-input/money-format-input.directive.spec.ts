import { DateFormatInputDirective } from './money-format-input.directive';

describe('DateFormatInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFormatInputDirective();
    expect(directive).toBeTruthy();
  });
});
